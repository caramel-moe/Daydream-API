--- a/src/main/java/io/papermc/paper/connection/PlayerCommonConnection.java
+++ b/src/main/java/io/papermc/paper/connection/PlayerCommonConnection.java
@@ -43,4 +_,30 @@
      * @return the client option value of the player
      */
     <T> T getClientOption(ClientOption<T> type);
+
+    // Daydream start - Add more ResourcePack API
+    /**
+     * 적용된 리소스팩 목록을 가져옵니다.
+     *
+     * @return 리소스팩 목록
+     */
+    java.util.Collection<? extends moe.caramel.daydream.player.ResourcePack> getAppliedResourcePacks();
+
+    /**
+     * 리소스팩 적용 상태를 가져옵니다.
+     *
+     * @param uuid 리소스팩의 고유 ID
+     * @return 적용을 하지 않았거나, 클라이언트에게 응답을 받지 않은 경우 {@code null}을 반환할 수도 있습니다.
+     */
+    org.bukkit.event.player.PlayerResourcePackStatusEvent.@org.jspecify.annotations.Nullable Status getResourcePackStatus(java.util.UUID uuid);
+    // Daydream end - Add more ResourcePack API
+
+    // Daydream start - Add Network API
+    /**
+     * 이 클라이언트 연결의 netty 채널을 가져옵니다.
+     *
+     * @return netty 채널
+     */
+    io.netty.channel.Channel getChannel();
+    // Daydream end - Add Network API
 }
